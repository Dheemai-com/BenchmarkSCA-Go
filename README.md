# BenchmarkSCA-Go

> **Warning:** this repository intentionally pins vulnerable module versions for scanner
> evaluation. Do not build or execute it.

A Go Modules SCA accuracy corpus for Vybscan. `ground-truth.json` is the independent
vulnerable/clean oracle. This repository remains internal until all labels are freshly reverified
and transitive module cases are added.
